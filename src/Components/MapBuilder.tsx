import React from 'react';
import Map from './Map';

const MapBuilder: React.FC = () => (
    <div>
        <header>
            <h1>Map Builder</h1>
        </header>
        <Map />
    </div>
);

export default MapBuilder;
